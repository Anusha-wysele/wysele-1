import React from 'react';

const Cylinderr = ({ className = "" }) => {
    return (
        // Renders a solid shape using the site's main maroon/red brand color
        <div className={`bg-[#800000] ${className}`}>
            {/* Content can be passed or it acts as a decorative block */}
        </div>
    );
};

export default Cylinderr;
